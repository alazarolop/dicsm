## @knitr spatial ####

# library(RSAGA)
# env <- rsaga.env(root = "/usr/local/opt/saga-gis", # Path to updated version SAGA GIS in macOS, instead of LTS
#                  parallel = TRUE)

library(rgdal)
library(raster)
library(sf)

# mapping
library(tmap)